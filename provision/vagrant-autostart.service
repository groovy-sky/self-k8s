[Unit]
Description=Starts vagrant VMs
After=syslog.target network.target open-vm-tools.service mnt.mount

[Service]
User=root
Group=root
ExecStart=/bin/sh -c 'cd /mnt/data/kubernetes-setup && /usr/bin/vagrant up'

[Install]
WantedBy=multi-user.target